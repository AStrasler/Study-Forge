# Study Forge dependencies
# Keep this list minimal. Add libraries only when they are actually used.

# Configuration
python-dotenv>=1.0.0

# HTTP clients (used by providers and Notion)
requests>=2.31.0
httpx>=0.27.0

# Document extraction (PDF / DOCX / PPTX)
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=1.0.0

# Better PDF text + render pages for OCR
pymupdf>=1.24.0

# Spreadsheets
openpyxl>=3.1.0
# Optional legacy .xls: xlrd>=2.0.0

# OCR (also needs system package: tesseract-ocr)
pillow>=10.0.0
pytesseract>=0.3.10

# Notion client
notion-client>=2.2.0
